SELECT * FROM emp WHERE substr(HIREDATE, 1, 4)=2014;

SELECT * FROM emp WHERE ENAME LIKE "%S%";

SELECT * FROM emp WHERE isnull(COMM)=TRUE;

SELECT avg(SAL) AVG, max(SAL) MAX, min(SAL) MIN, count(*) CNT FROM emp group by DEPTNO;

SELECT ENAME, DEPTNO, SAL, (SAL*12)+(SAL*1.5) YEARSAL FROM emp WHERE DEPTNO=30;

SELECT ENAME, SAL, SAL*0.15 FROM emp WHERE SAL>=2000;

SELECT DEPTNO, avg(SAL) AVG, sum(SAL) SUM, max(SAL) MAX, min(SAL) MIN FROM emp group by DEPTNO order by AVG DESC;
